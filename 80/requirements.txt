# 工业物联网终端增量固件升级与版本管控工具
# 依赖包列表

# MQTT 协议支持 (可选)
paho-mqtt>=1.6.0

# CoAP 协议支持 (可选)
CoAPthon3>=1.0.1

# 命令行补全 (可选)
argcomplete>=2.0.0

# 进度条显示 (可选)
tqdm>=4.64.0

# YAML 配置支持 (可选)
PyYAML>=6.0

# 类型提示支持
typing-extensions>=4.0.0
