FROM openjdk:8-jre-alpine

LABEL maintainer="iot-gateway"

WORKDIR /app

COPY iot-gateway-gateway/target/iot-gateway-gateway-1.0.0.jar app.jar

EXPOSE 9000

ENV JVM_OPTS="-Xms256m -Xmx512m"

ENTRYPOINT ["sh", "-c", "java $JVM_OPTS -Djava.security.egd=file:/dev/./urandom -jar app.jar"]
